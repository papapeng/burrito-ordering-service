# burrito-ordering-service


1. Postman test collection as a json file is provided in root directory which can be used to test against local running burrito-ordering-service.

2. jwt token based security mechnism can be implemented. But it is skipped due to only 1 hour time limitation.
